# StockCal API 資料更新指南

> 如何更新每日熱點、每日策略和月度事件資料

## 📅 更新頻率

| 資料類型 | 更新頻率 | 檔案名稱 |
|---------|---------|---------|
| 月度事件 | **每月更新** | `data_events.json` |
| 每日熱點 | **每日更新** | `data_hot_trends.json` |
| 每日策略 | **每日更新** | `data_strategies.json` |

---

## 🔄 更新步驟

### 方法 A：透過 GitHub 網頁介面（推薦）

1. **前往 GitHub 儲存庫**
   - https://github.com/simon4657/stockcal-api

2. **編輯資料檔案**
   - 點擊要更新的檔案（例如 `data_hot_trends.json`）
   - 點擊右上角的「✏️ Edit this file」按鈕
   - 修改內容
   - 點擊「Commit changes」

3. **自動部署**
   - Render 會自動偵測 GitHub 的變更
   - 約 1-2 分鐘後，新資料就會生效

### 方法 B：本地更新並推送

```bash
# 1. Clone 儲存庫（首次）
git clone https://github.com/simon4657/stockcal-api.git
cd stockcal-api

# 2. 編輯資料檔案
nano data_hot_trends.json  # 或使用您喜歡的編輯器

# 3. 提交並推送
git add .
git commit -m "Update hot trends for 2025-11-24"
git push origin master
```

---

## 📝 資料格式說明

### 1. 每日熱點（data_hot_trends.json）

```json
[
  {
    "id": "hot-1",                    // 唯一 ID
    "name": "BBU 電池模組",            // 熱點名稱
    "strength": 95,                   // 強度 (0-100)
    "trend": "up",                    // 趨勢: up/down/neutral/volatile
    "stocks": [                       // 相關個股列表
      "AES-KY (6781)",
      "順達 (3211)"
    ],
    "reason": "輝達 GB200...",        // 原因說明
    "updatedAt": "2025-11-24"         // 更新日期 (YYYY-MM-DD)
  }
]
```

**更新要點：**
- ✅ 每日更新 `updatedAt` 日期
- ✅ 調整 `strength` 反映當日資金強度
- ✅ 更新 `stocks` 列表
- ✅ 修改 `reason` 說明當日變化

### 2. 每日策略（data_strategies.json）

```json
[
  {
    "id": "st-1",                     // 唯一 ID
    "title": "感恩節前量縮觀望",       // 策略標題
    "type": "neutral",                // 類型: bull/bear/neutral/volatile
    "desc": "美股週四休市...",        // 策略描述
    "risk": "低",                     // 風險等級: 低/中/高
    "target": "大盤指數",             // 關注目標
    "updatedAt": "2025-11-24"         // 更新日期
  }
]
```

**更新要點：**
- ✅ 每日更新 `updatedAt` 日期
- ✅ 根據當日盤勢調整策略內容
- ✅ 修改 `type` 和 `risk` 反映市場狀況

### 3. 月度事件（data_events.json）

```json
[
  {
    "id": "11-24-bbu",                // 唯一 ID
    "date": "2025-11-24",             // 事件日期 (YYYY-MM-DD)
    "title": "BBU 資金狂熱",          // 事件標題
    "market": "TW",                   // 市場: US/TW/Global
    "type": "hot",                    // 類型: critical/hot/corporate/macro/holiday
    "trend": "bull",                  // 趨勢: bull/bear/neutral/volatile
    "relatedStocks": [                // 相關個股（可選）
      "AES-KY (6781)"
    ],
    "description": "輝達 GB200...",   // 事件描述
    "strategy": "目前處於主升段..."   // 操作策略
  }
]
```

**更新要點：**
- ✅ 每月初更新當月及未來兩個月的事件
- ✅ 刪除已過期的事件
- ✅ 添加新的重要事件

---

## 🎯 每日更新檢查清單

### 每日早盤前（9:00 AM 前）

- [ ] 更新 `data_hot_trends.json`
  - [ ] 調整各族群的 `strength` 數值
  - [ ] 更新 `stocks` 列表
  - [ ] 修改 `reason` 說明
  - [ ] 更新 `updatedAt` 為當日日期

- [ ] 更新 `data_strategies.json`
  - [ ] 根據前一日盤勢調整策略
  - [ ] 更新 `desc` 內容
  - [ ] 調整 `risk` 等級
  - [ ] 更新 `updatedAt` 為當日日期

### 每月初（1-3 號）

- [ ] 更新 `data_events.json`
  - [ ] 刪除上個月的事件
  - [ ] 添加下個月的重要事件
  - [ ] 確認財報日期、經濟數據公布日期

---

## 🔍 驗證更新

更新後，請訪問以下 URL 確認資料已更新：

- **熱點資料**: https://stockcal-api.onrender.com/api/hot-trends
- **策略資料**: https://stockcal-api.onrender.com/api/strategies
- **事件資料**: https://stockcal-api.onrender.com/api/events

---

## ⚠️ 注意事項

1. **JSON 格式**：確保 JSON 格式正確，可使用 [JSONLint](https://jsonlint.com/) 驗證
2. **日期格式**：統一使用 `YYYY-MM-DD` 格式
3. **中文編碼**：確保檔案使用 UTF-8 編碼
4. **部署時間**：Render 自動部署約需 1-2 分鐘

---

## 📞 技術支援

如有問題，請查看：
- **API 文件**: https://stockcal-api.onrender.com/docs
- **GitHub Issues**: https://github.com/simon4657/stockcal-api/issues
